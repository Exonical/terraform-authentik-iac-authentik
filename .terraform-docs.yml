formatter: markdown table

version: ">= 0.16.0"

sections:
  show:
    - requirements
    - providers
    - inputs
    - outputs
    - modules
    - resources

output:
  file: README.md
  mode: replace

sort:
  enabled: true
  by: required